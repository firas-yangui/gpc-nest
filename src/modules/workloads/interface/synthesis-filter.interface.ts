export interface SynthesisFilter {
  code: string;
  description: string;
  domaine: string;
  thirdparties: number[];
  portfolios: number[];
  plans: number[];
  subnatures: number[];
  partners: number[];
}
